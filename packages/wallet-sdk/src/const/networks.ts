import { arbitrum, base, arbitrumSepolia, baseSepolia } from 'viem/chains';

export const supportedNetworks = [arbitrum, base, arbitrumSepolia, baseSepolia];

export const defaultNetwork = arbitrumSepolia;
